[permissions]
allow = ["Bash=cd \"D:/MyProjects/MacroMax\" && for f in src/hooks/useFoodSearch.ts src/lib/nutrition.ts src/lib/fatSecret.ts src/lib/openFoodFacts.ts; do echo \"=========== $f ===========\"; cat -n \"$f\"; done", "Bash=cd \"D:/MyProjects/MacroMax\" && for f in \"app/(tabs)/analytics.tsx\" \"app/(tabs)/settings.tsx\" \"app/(tabs)/recipe.tsx\" src/lib/fallbackFoods.ts; do echo \"=========== $f ===========\"; cat -n \"$f\"; done", "Bash=cd \"D:/MyProjects/MacroMax\" && echo \"=== expo-router exports (useFocusEffect?) ===\" && ls node_modules/expo-router/ && echo \"--- build/index.d.ts ---\" && grep -n \"export\" node_modules/expo-router/build/index.d.ts 2>/dev/null | head -40 && echo \"=== versions ===\" && node -e \"const p=require('./package.json');const r=n=>{try{return require('./node_modules/'+n+'/package.json').version}catch(e){return 'MISSING'}};['expo','expo-router','expo-font','expo-splash-screen','@expo/vector-icons','nativewind','react-native','react-native-reanimated','react-native-safe-area-context','react-native-screens','expo-camera','@react-navigation/bottom-tabs','@react-navigation/native'].forEach(n=>console.log(n+': '+r(n)))\"", "Bash=cd \"D:/MyProjects/MacroMax\" && echo \"=== does babel-preset-expo auto-add reanimated/worklets? ===\" && grep -rn \"reanimated\\|worklets\" node_modules/babel-preset-expo/build/index.js | head -20 && echo \"--- nativewind/babel preset content ---\" && cat node_modules/nativewind/babel.js && echo \"--- css-interop ---\" && node -e \"console.log(require('./node_modules/react-native-css-interop/package.json').version)\"", "Bash=cd \"$TEMP\" && mkdir -p mmprobe && cat > mmprobe/probe.js <<'EOF'\nconst path = require(\"path\");\nconst repo = process.argv[2];\nconst babel = require(path.join(repo, \"node_modules\", \"@babel\", \"core\"));\nconst code = 'import { View } from \"react-native\";\\nexport const A = () => <View className=\"flex-1 bg-surface\" />;\\n';\nconst out = babel.transformSync(code, {\n  filename: path.join(repo, \"probe.tsx\"),\n  cwd: repo,\n  configFile: path.join(repo, \"babel.config.js\"),\n  babelrc: false,\n  caller: { name: \"metro\", platform: \"android\", supportsStaticESM: false },\n});\nconst jsxImport = (out.code.match(/require\\(\"([^\"]*jsx-runtime[^\"]*)\"\\)/) || [, \"<none>\"])[1];\nconsole.log(\"JSX runtime:\", jsxImport);\nconsole.log(\"className preserved in output:\", /className/.test(out.code));\nEOF\nnode \"$TEMP/mmprobe/probe.js\" \"D:/MyProjects/MacroMax\"", "Bash=cd \"$TEMP\" && mkdir -p mmprobe && cat > mmprobe/scan.js <<'EOF'\nconst fs = require(\"fs\"), path = require(\"path\");\nconst root = process.argv[2];\nconst SKIP = new Set([\"node_modules\", \".git\", \"build\", \".expo\", \".gradle\", \".idea\", \"android\", \"assets\"]);\nconst files = [];\nfunction walk(d) {\n  for (const e of fs.readdirSync(d, { withFileTypes: true })) {\n    if (SKIP.has(e.name)) continue;\n    const p = path.join(d, e.name);\n    if (e.isDirectory()) walk(p);\n    else if (/\\.(ts|tsx)$/.test(e.name)) files.push(p);\n  }\n}\nfor (const d of [\"app\", \"components\", \"src\", \"test\"]) {\n  const p = path.join(root, d);\n  if (fs.existsSync(p)) walk(p);\n}\nconst rel = (f) => path.relative(root, f).replace(/\\\\/g, \"/\");\nconst text = {};\nfor (const f of files) text[rel(f)] = fs.readFileSync(f, \"utf8\");\nconst names = Object.keys(text);\nconst all = names.map((n) => text[n]).join(\"\\n\");\n\n// ---- imports -> used modules\nconst imported = new Set();\nconst pkgImports = new Set();\nconst re = /(?:import|export)[^;]*?from\\s+[\"']([^\"']+)[\"']|require\\(\\s*[\"']([^\"']+)[\"']\\s*\\)|import\\(\\s*[\"']([^\"']+)[\"']\\s*\\)/g;\nfor (const n of names) {\n  let m;\n  re.lastIndex = 0;\n  while ((m = re.exec(text[n]))) {\n    const spec = m[1] || m[2] || m[3];\n    if (!spec) continue;\n    if (spec.startsWith(\".\") || spec.startsWith(\"@/\")) {\n      const base = spec.startsWith(\"@/\") ? path.join(root, spec.slice(2)) : path.resolve(path.dirname(path.join(root, n)), spec);\n      for (const cand of [base + \".ts\", base + \".tsx\", path.join(base, \"index.ts\"), path.join(base, \"index.tsx\"), base]) {\n        if (fs.existsSync(cand) && fs.statSync(cand).isFile()) { imported.add(rel(cand)); break; }\n      }\n    } else if (!spec.startsWith(\"expo-router/entry\")) {\n      pkgImports.add(spec.startsWith(\"@\") ? spec.split(\"/\").slice(0, 2).join(\"/\") : spec.split(\"/\")[0]);\n    }\n  }\n}\n\n// ---- exported symbols never referenced outside their own file\nconst unusedExports = [];\nfor (const n of names) {\n  const body = text[n];\n  const names0 = [];\n  const patterns = [\n    /export\\s+(?:async\\s+)?function\\s+(\\w+)/g,\n    /export\\s+(?:const|let|var)\\s+(\\w+)/g,\n    /export\\s+class\\s+(\\w+)/g,\n    /export\\s+(?:type|interface|enum)\\s+(\\w+)/g,\n    /export\\s*\\{([^}]+)\\}/g,\n  ];\n  for (const p of patterns) {\n    let m;\n    p.lastIndex = 0;\n    while ((m = p.exec(body))) {\n      if (m[1].includes(\",\")) m[1].split(\",\").forEach((s) => { const t = s.split(/\\s+as\\s+/).pop().trim(); if (/^\\w+$/.test(t)) names0.push(t); });\n      else names0.push(m[1]);\n    }\n  }\n  for (const sym of new Set(names0)) {\n    const outside = names.filter((o) => o !== n).some((o) => new RegExp(\"\\\\b\" + sym + \"\\\\b\").test(text[o]));\n    const insideCount = (body.match(new RegExp(\"\\\\b\" + sym + \"\\\\b\", \"g\")) || []).length;\n    if (!outside) unusedExports.push({ file: n, sym, internalUses: insideCount - 1 });\n  }\n}\n\n// ---- unused files (never imported). app/** are expo-router routes: always used.\nconst unusedFiles = names.filter((n) => !n.startsWith(\"app/\") && !n.startsWith(\"test/\") && !imported.has(n));\n\n// ---- top-level declarations used only once in their own file (dead local helpers)\nconst deadLocals = [];\nfor (const n of names) {\n  const lines = text[n].split(\"\\n\");\n  lines.forEach((line, i) => {\n    const m = /^(?:async\\s+)?function\\s+(\\w+)|^const\\s+(\\w+)\\s*=/.exec(line);\n    if (!m) return;\n    const sym = m[1] || m[2];\n    if (/^export/.test(line)) return;\n    if (sym === \"styles\" || sym === \"re\") return;\n    const count = (text[n].match(new RegExp(\"\\\\b\" + sym + \"\\\\b\", \"g\")) || []).length;\n    if (count <= 1) deadLocals.push({ file: n, line: i + 1, sym });\n  });\n}\n\n// ---- npm deps never imported\nconst pkg = JSON.parse(fs.readFileSync(path.join(root, \"package.json\"), \"utf8\"));\nconst deps = Object.keys(pkg.dependencies || {});\nconst unusedDeps = deps.filter((d) => !pkgImports.has(d) && !names.some((n) => new RegExp('[\"\\']' + d.replace(/[/@.]/g, \"\\\\$&\")).test(text[n])));\n\nconsole.log(\"== FILES never imported (excluding app/ routes) ==\");\nunusedFiles.forEach((f) => console.log(\"  \" + f));\nconsole.log(\"\\n== EXPORTS never referenced outside their file ==\");\nunusedExports.forEach((u) => console.log(`  ${u.file}  ::  ${u.sym}   (internal uses: ${u.internalUses})`));\nconsole.log(\"\\n== TOP-LEVEL declarations used only once in file ==\");\ndeadLocals.forEach((d) => console.log(`  ${d.file}:${d.line}  ${d.sym}`));\nconsole.log(\"\\n== npm deps not imported anywhere ==\");\nunusedDeps.forEach((d) => console.log(\"  \" + d));\nconsole.log(\"\\nfiles scanned:\", names.length);\nEOF\nnode \"$TEMP/mmprobe/scan.js\" \"D:/MyProjects/MacroMax\"", "Bash=cd \"$TEMP\" && cat > mmprobe/scan.js <<'EOF'\nconst fs = require(\"fs\"), path = require(\"path\");\nconst root = process.argv[2];\nconst BS = String.fromCharCode(92);\nconst TAB = String.fromCharCode(9);\nconst W = \"[A-Za-z0-9_]\";\nconst wb = (s) => BS + \"b\" + s + BS + \"b\";\nconst SP = \"[\" + TAB + \" ]\";\nconst SKIP = new Set([\"node_modules\", \".git\", \"build\", \".expo\", \".gradle\", \".idea\", \"android\", \"assets\"]);\nconst files = [];\nfunction walk(d) {\n  for (const e of fs.readdirSync(d, { withFileTypes: true })) {\n    if (SKIP.has(e.name)) continue;\n    const p = path.join(d, e.name);\n    if (e.isDirectory()) walk(p);\n    else {\n      const ext = path.extname(e.name);\n      if (ext === \".ts\" || ext === \".tsx\") files.push(p);\n    }\n  }\n}\nfor (const d of [\"app\", \"components\", \"src\", \"test\"]) {\n  const p = path.join(root, d);\n  if (fs.existsSync(p)) walk(p);\n}\nconst rel = (f) => path.relative(root, f).split(path.sep).join(\"/\");\nconst text = {};\nfor (const f of files) text[rel(f)] = fs.readFileSync(f, \"utf8\");\nconst names = Object.keys(text);\nconst imported = new Set();\nconst pkgImports = new Set();\nconst res = [\n  new RegExp(\"(?:from)\" + SP + \"*\" + \"[\\\"']([^\\\"']+)[\\\"']\", \"g\"),\n  new RegExp(\"require\" + SP + \"*\" + BS + \"(\" + SP + \"*[\\\"']([^\\\"']+)[\\\"']\", \"g\"),\n  new RegExp(\"import\" + BS + \"(\" + SP + \"*[\\\"']([^\\\"']+)[\\\"']\", \"g\"),\n];\nfor (const n of names) {\n  for (const re of res) {\n    let m;\n    re.lastIndex = 0;\n    while ((m = re.exec(text[n]))) {\n      const spec = m[1] || m[2];\n      if (!spec) continue;\n      if (spec.startsWith(\".\") || spec.startsWith(\"@/\")) {\n        const base = spec.startsWith(\"@/\") ? path.join(root, spec.slice(2)) : path.resolve(path.dirname(path.join(root, n)), spec);\n        for (const cand of [base + \".ts\", base + \".tsx\", path.join(base, \"index.ts\"), path.join(base, \"index.tsx\")]) {\n          if (fs.existsSync(cand)) { imported.add(rel(cand)); break; }\n        }\n      } else {\n        pkgImports.add(spec.startsWith(\"@\") ? spec.split(\"/\").slice(0, 2).join(\"/\") : spec.split(\"/\")[0]);\n      }\n    }\n  }\n}\nconst unusedExports = [];\nfor (const n of names) {\n  const body = text[n];\n  const syms = [];\n  const pats = [\n    new RegExp(\"export\" + SP + \"+(?:async\" + SP + \"+)?function\" + SP + \"+(\" + W + \"+)\", \"g\"),\n    new RegExp(\"export\" + SP + \"+(?:const|let|var)\" + SP + \"+(\" + W + \"+)\", \"g\"),\n    new RegExp(\"export\" + SP + \"+class\" + SP + \"+(\" + W + \"+)\", \"g\"),\n    new RegExp(\"export\" + SP + \"+(?:type|interface|enum)\" + SP + \"+(\" + W + \"+)\", \"g\"),\n    new RegExp(\"export\" + SP + \"*\" + BS + \"{([^}]+)\" + BS + \"}\", \"g\"),\n  ];\n  for (const p of pats) {\n    let m;\n    p.lastIndex = 0;\n    while ((m = p.exec(body))) syms.push(...m[1].split(\",\").map((s) => s.trim().split(\" as \").pop().trim()));\n  }\n  for (const sym of new Set(syms)) {\n    if (!new RegExp(\"^\" + W + \"+$\").test(sym)) continue;\n    const outside = names.filter((o) => o !== n).some((o) => new RegExp(wb(sym)).test(text[o]));\n    const inside = (body.match(new RegExp(wb(sym), \"g\")) || []).length;\n    if (!outside) unusedExports.push({ file: n, sym, internalUses: inside - 1 });\n  }\n}\nconst unusedFiles = names.filter((n) => !n.startsWith(\"app/\") && !n.startsWith(\"test/\") && !imported.has(n));\nconst deadLocals = [];\nfor (const n of names) {\n  text[n].split(\"\\n\").forEach((line, i) => {\n    const m = new RegExp(\"^(?:async\" + SP + \"+)?function\" + SP + \"+(\" + W + \"+)|^const\" + SP + \"+(\" + W + \"+)\" + SP + \"*=\").exec(line);\n    if (!m) return;\n    if (line.startsWith(\"export\")) return;\n    const sym = m[1] || m[2];\n    const count = (text[n].match(new RegExp(wb(sym), \"g\")) || []).length;\n    if (count <= 1) deadLocals.push({ file: n, line: i + 1, sym });\n  });\n}\nconst pkg = JSON.parse(fs.readFileSync(path.join(root, \"package.json\"), \"utf8\"));\nconst deps = Object.keys(pkg.dependencies || {});\nconst unusedDeps = deps.filter((d) => !pkgImports.has(d) && !names.some((n) => text[n].includes(\"\\\"\" + d + \"\\\"\") || text[n].includes(\"\\\"\" + d + \"/\") || text[n].includes(\"'\" + d + \"'\") || text[n].includes(\"'\" + d + \"/\")));\nconsole.log(\"== FILES never imported (app/ routes excluded) ==\");\nunusedFiles.forEach((f) => console.log(\"  \" + f));\nconsole.log(\"\");\nconsole.log(\"== EXPORTS never referenced outside their file ==\");\nunusedExports.forEach((u) => console.log(\"  \" + u.file + \" :: \" + u.sym + \"  (internal uses: \" + u.internalUses + \")\"));\nconsole.log(\"\");\nconsole.log(\"== TOP-level declarations used only once (dead local) ==\");\ndeadLocals.forEach((d) => console.log(\"  \" + d.file + \":\" + d.line + \"  \" + d.sym));\nconsole.log(\"\");\nconsole.log(\"== npm deps not imported anywhere ==\");\nunusedDeps.forEach((d) => console.log(\"  \" + d));\nconsole.log(\"\");\nconsole.log(\"files scanned: \" + names.length);\nEOF\nnode \"$TEMP/mmprobe/scan.js\" \"D:/MyProjects/MacroMax\"", "Bash=cd \"D:/MyProjects/MacroMax\" && for sym in MACRO_SOFTS MacroKey getDailyLog getRecipeIngredients SERVING_INPUT_HINT_COLOR saveProfile REMOTE_TIMEOUT_MS TAB_ITEMS TAB_BAR_CONTENT_HEIGHT SearchCategory SyncReportOptions EXPO_PUBLIC_USDA; do echo \"--- $sym ---\"; grep -rn \"$sym\" --include=*.ts --include=*.tsx --include=*.js --include=*.json --include=*.md --include=*.yml . 2>/dev/null | grep -v \"^./node_modules\" | grep -v \"^./package-lock.json\" | grep -v \"^./android/\"; done", "Bash=cd \"$TEMP\" && cat > mmprobe/scan2.js <<'EOF'\nconst fs = require(\"fs\"), path = require(\"path\");\nconst root = process.argv[2];\nconst BS = String.fromCharCode(92);\nconst WB = BS + \"b\";\nconst read = (p) => fs.readFileSync(path.join(root, p), \"utf8\");\nconst colors = read(\"src/theme/colors.ts\");\nconst shadows = read(\"src/theme/shadows.ts\");\nconst keys = (src, obj) => {\n  const m = new RegExp(\"export const \" + obj + \" = \" + BS + \"{([^}]*)\" + BS + \"}\", \"s\").exec(src);\n  if (!m) return [];\n  return m[1].split(\"\\n\").map((l) => l.trim().split(\":\")[0].trim()).filter((k) => /^[A-Za-z0-9_]+$/.test(k));\n};\nconst groups = {\n  COLORS: keys(colors, \"COLORS\"),\n  MACRO_COLORS: keys(colors, \"MACRO_COLORS\"),\n  MACRO_TRACKS: keys(colors, \"MACRO_TRACKS\"),\n  MACRO_SOFTS: keys(colors, \"MACRO_SOFTS\"),\n  CHART_COLORS: keys(colors, \"CHART_COLORS\"),\n  SHADOWS: keys(shadows, \"SHADOWS\"),\n};\nconst files = [];\nconst SKIP = new Set([\"node_modules\", \".git\", \"build\", \".expo\", \".gradle\", \".idea\", \"android\", \"assets\"]);\nfunction walk(d) {\n  for (const e of fs.readdirSync(d, { withFileTypes: true })) {\n    if (SKIP.has(e.name)) continue;\n    const p = path.join(d, e.name);\n    if (e.isDirectory()) walk(p);\n    else if ([\".ts\", \".tsx\", \".js\"].includes(path.extname(e.name))) files.push(p);\n  }\n}\n[\"app\", \"components\", \"src\"].forEach((d) => walk(path.join(root, d)));\nconst corpus = files.map((f) => fs.readFileSync(f, \"utf8\")).join(\"\\n\");\nfor (const [group, ks] of Object.entries(groups)) {\n  const unused = ks.filter((k) => !(corpus.match(new RegExp(WB + group + BS + \".\" + k + WB, \"g\")) || []).length);\n  console.log(group + \": \" + ks.length + \" keys, UNUSED -> \" + (unused.length ? unused.join(\", \") : \"(none)\"));\n}\nEOF\nnode \"$TEMP/mmprobe/scan2.js\" \"D:/MyProjects/MacroMax\" && echo \"--- DESIGN.md exists? ---\" && ls DESIGN.md 2>&1 | head -2 && echo \"--- design doc references ---\" && grep -rn \"DESIGN.md\\|FIGMA\\|TODO\\|FIXME\\|XXX\\|deprecated\\|DEPRECATED\" --include=*.ts --include=*.tsx --include=*.js --include=*.md . 2>/dev/null | grep -v \"^./node_modules\" | grep -v \"^./package-lock.json\" | grep -v \"^./android/\"", "Bash=cd /d/MyProjects/MacroMax && echo \"=== installed versions ===\" && node -e \"\nconst p=require('./package.json');\nconst r=n=>{try{return require('./node_modules/'+n+'/package.json').version}catch(e){return 'MISSING'}};\n['expo','expo-router','expo-splash-screen','expo-status-bar','expo-font','nativewind','react-native','react-native-reanimated','react-native-worklets','react-native-safe-area-context','react-native-screens','@react-navigation/native','@react-navigation/bottom-tabs','react-native-css-interop','react-native-gesture-handler'].forEach(n=>console.log(n+': '+r(n)));\n\" && echo \"=== reanimated peer/deps ===\" && node -e \"const p=require('./node_modules/react-native-reanimated/package.json'); console.log(JSON.stringify({version:p.version,peerDependencies:p.peerDependencies,dependencies:p.dependencies},null,2))\" && echo \"=== worklets pkg deps ===\" && node -e \"const p=require('./node_modules/react-native-worklets/package.json'); console.log(JSON.stringify({version:p.version,peerDependencies:p.peerDependencies,dependencies:p.dependencies},null,2))\"", "Bash=cd /d/MyProjects/MacroMax && for f in \"app/_layout.tsx\" \"app/login.tsx\" \"components/RequireAuth.tsx\" \"src/hooks/useAuth.ts\" \"src/hooks/useProfile.ts\"; do echo \"=========== $f ===========\"; cat -n \"$f\"; done", "Bash=cd /d/MyProjects/MacroMax && for f in \"app/(tabs)/_layout.tsx\" \"app/(tabs)/index.tsx\" \"src/hooks/useDailyLog.ts\"; do echo \"=========== $f ===========\"; cat -n \"$f\"; done", "Bash=cd /d/MyProjects/MacroMax && echo \"=== expo pkg deps ===\" && node -e \"const p=require('./node_modules/expo/package.json');console.log(Object.keys(p.dependencies).join('\\n'))\"", "Bash=cd /d/MyProjects/MacroMax && echo \"=== splash-screen dir ===\" && (ls -d node_modules/expo-splash-screen || echo MISSING) && echo \"=== git log package.json ===\" && git log --oneline -5 -- package.json && echo \"=== HEAD~1 package.json deps ===\" && git show HEAD~1:package.json", "Bash=cd /d/MyProjects/MacroMax && for f in \"app/(tabs)/settings.tsx\" \"app/(tabs)/log.tsx\"; do echo \"=========== $f ===========\"; cat -n \"$f\"; done", "Bash=cd /d/MyProjects/MacroMax && echo \"### worklets in commits ###\" && for c in HEAD HEAD~1 809c2bb 9a90bee; do printf \"%s: \" \"$c\"; git show \"$c:package.json\" | grep -c \"react-native-worklets\"; done; echo \"### full log ###\" && git log --oneline --all | head -20", "Bash=cd /d/MyProjects/MacroMax && echo \"### workflows ###\" && ls -R .github && for f in .github/workflows/*; do echo \"=== $f ===\"; cat \"$f\"; done", "Bash=cd /d/MyProjects/MacroMax && echo \"### nativewind/babel exists? ###\" && ls node_modules/nativewind/babel.js node_modules/nativewind/babel* 2>&1 | head -5 && cat node_modules/nativewind/babel.js && echo \"### nativewind pkg exports ###\" && node -e \"const p=require('./node_modules/nativewind/package.json');console.log(JSON.stringify(p.exports||p.files||{},null,1).slice(0,600));console.log('peer:',JSON.stringify(p.peerDependencies))\"", "Bash=cd /d/MyProjects/MacroMax && echo \"### useFocusEffect anywhere in expo-router? ###\" && grep -rln \"useFocusEffect\" node_modules/expo-router/ 2>/dev/null | head -10 && echo \"### expo-router index.d.ts exports (head) ###\" && head -60 node_modules/expo-router/build/index.d.ts && echo \"### runtime check ###\" && node -e \"const fs=require('fs');const s=fs.readFileSync('node_modules/expo-router/build/index.js','utf8');console.log('has useFocusEffect:', s.includes('useFocusEffect'));console.log(s.slice(0,800))\"", "Bash=cd /d/MyProjects/MacroMax && cat > ~/.mm-export/probe.js <<'EOF'\nconst path = require(\"path\");\nconst repo = \"D:/MyProjects/MacroMax\";\nconst babel = require(path.join(repo, \"node_modules\", \"@babel\", \"core\"));\nconst code = [\n  'import { View, Text } from \"react-native\";',\n  'export const A = () => <View className=\"flex-1 items-center bg-surface\"><Text className=\"text-ink\">hi</Text></View>;',\n].join(\"\\n\");\nconst out = babel.transformSync(code, {\n  filename: path.join(repo, \"probe.tsx\"),\n  cwd: repo,\n  configFile: path.join(repo, \"babel.config.js\"),\n  babelrc: false,\n  caller: { name: \"metro\", platform: \"android\", supportsStaticESM: false },\n});\nconst jsxImport = (out.code.match(/require\\(\"([^\"]*jsx-runtime[^\"]*)\"\\)/) || [, \"<none>\"])[1];\nconsole.log(\"jsx runtime        :\", jsxImport);\nconsole.log(\"css-interop plugin :\", /css-interop|cssInterop/.test(out.code) ? \"YES (in output)\" : \"not visible in output\");\nconsole.log(\"className kept     :\", /className/.test(out.code));\nconsole.log(\"--- output ---\");\nconsole.log(out.code);\nEOF\nnode ~/.mm-export/probe.js; echo \"--- bundle string check ---\"; strings ~/.mm-export/_expo/static/js/android/*.hbc 2>/dev/null | grep -c \"cssInterop\" || grep -c \"cssInterop\" ~/.mm-export/_expo/static/js/android/*.hbc", "Bash=cd /d/MyProjects/MacroMax && cat > ~/.mm-export/probe-old.js <<'EOF'\nconst path = require(\"path\");\nconst repo = \"D:/MyProjects/MacroMax\";\nconst babel = require(path.join(repo, \"node_modules\", \"@babel\", \"core\"));\nconst code = 'import { View } from \"react-native\";\\nexport const A = () => <View className=\"flex-1\" />;';\nconst out = babel.transformSync(code, {\n  filename: path.join(repo, \"probe.tsx\"),\n  cwd: repo,\n  configFile: false,\n  babelrc: false,\n  presets: [require.resolve(path.join(repo, \"node_modules\", \"babel-preset-expo\"))],\n  caller: { name: \"metro\", platform: \"android\", supportsStaticESM: false },\n});\nconst jsxImport = (out.code.match(/require\\(\"([^\"]*jsx-runtime[^\"]*)\"\\)/) || [, \"<none>\"])[1];\nconsole.log(\"OLD config (babel-preset-expo only) -> jsx runtime:\", jsxImport);\nEOF\nnode ~/.mm-export/probe-old.js", "Bash=cd /d/MyProjects/MacroMax && echo \"### stray 'worklets' references (repo, excl. node_modules/.git/android) ###\" && grep -rl \"worklets\" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=android --exclude-dir=.expo 2>/dev/null | head; echo \"(end)\"; echo \"### lock vs package.json ###\" && node -e \"const p=require('./package.json'),l=require('./package-lock.json');const a=Object.keys(p.dependencies||{}).sort().join(','),b=Object.keys(l.packages[''].dependencies||{}).sort().join(',');console.log('root deps identical:',a===b);console.log('worklets entries in lock:',Object.keys(l.packages).filter(k=>k.includes('worklets')).length)\"", "Bash=cd \"D:/MyProjects/MacroMax\" && echo \"---TREE app---\" && find app components src test -type f | head -60 && echo \"---node_modules top expo ver---\" && node -e \"console.log(require('./node_modules/expo/package.json').version); console.log(require('./node_modules/react-native/package.json').version)\" 2>&1"]
